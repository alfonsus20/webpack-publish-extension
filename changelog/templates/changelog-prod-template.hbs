# Changelog Production

{{#each releases}}
  {{#if href}}
    ## [{{title}}]({{href}}){{#if tag}} ({{isoDate}}){{/if}}
  {{else}}
    ## {{title}}{{#if tag}} ({{isoDate}}){{/if}}
  {{/if}}

  {{#if summary}}
    {{summary}}
  {{/if}}

  {{#each merges}}
    - {{message}} {{#if href}}[`#{{id}}`]({{href}}){{/if}}
  {{/each}}
  {{#each fixes}}
    - {{commit.subject}}{{#each fixes}} {{#if href}}[`#{{id}}`]({{href}}){{/if}}{{/each}}
  {{/each}}
  {{#each commits}}
    - {{#if breaking}}**Breaking change:** {{/if}}{{subject}} {{#if href}}[`{{shorthash}}`]({{href}}){{/if}}
  {{/each}}

{{/each}}

## 3.1.10:
- Remove JT middleware

## 3.1.9:
- Show auth error

## 3.1.8:
- New login flow
- Add support for CVFR
- Add support for Baosheng
- Search in the fulfillment dropdown
- Copy trace ID button

## 3.1.7:
- Re-enable MFA

## 3.1.6:
- Enable error message

## 3.1.5:
- Disabled JT middleware
- Disable MFA

## 3.1.4:
- Rollback to 3.1.2

## 3.1.3:
- JT Login with Middleware
- Whitelist Trigana V2

## 3.1.2:
- Support Login OTP
- Add support for TSY Aggregator

## 3.1.1:
- Split Logout tools and provider
- Fix receiver Storage ordering

## 3.1.0:
- Add Cognito w/ Multi-factor authentication (MFA) method (disable in default)
- Remove Auth0 authentication method
- Add support for Vietnam Airlines, Onefly, Unififi, PKFare

## 3.0.0:
- Update manifest version to v3
- Support for multiple chunk responses
- Fix some bugs

## 2.1.11:
- New Login Mechanism

## 2.1.2:
- Latest Working Version + Cognito Auth

## 2.0.8:
- Latest Working Version