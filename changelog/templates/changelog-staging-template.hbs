# Changelog Staging

{{#each releases}}
  {{#if href}}
    ## [{{title}}]({{href}}){{#if tag}} ({{isoDate}}){{/if}}
  {{else}}
    ## {{title}}{{#if tag}} ({{isoDate}}){{/if}}
  {{/if}}

  {{#if summary}}
    {{summary}}
  {{/if}}

  {{#each merges}}
    - {{message}} {{#if href}}[`#{{id}}`]({{href}}){{/if}}
  {{/each}}
  {{#each fixes}}
    - {{commit.subject}}{{#each fixes}} {{#if href}}[`#{{id}}`]({{href}}){{/if}}{{/each}}
  {{/each}}
  {{#each commits}}
    - {{#if breaking}}**Breaking change:** {{/if}}{{subject}} {{#if href}}[`{{shorthash}}`]({{href}}){{/if}}
  {{/each}}

{{/each}}

## 3.1.11:
- Remove JT middleware

## 3.1.10:
- Added traceId param

## 3.1.9:
- Show auth error

## 3.1.8:
- New login flow

## 3.1.7:
- Add support for CVFR
- Add support for Baosheng

## 3.1.6:
- Search in the fulfillment dropdown
- Copy trace ID button

## 3.1.5:
- Re-enable MFA

## 2.1.10:
- Bump loader-utils version to 1.4.2 to fix a vulnerability

## 2.1.9:
- Hide error message on change airline and buttons click
- Changed the response model to match the BE

## 2.1.8:
- Display error message

## 2.1.7:
- Disabled JT middleware
- Disable MFA

## 2.1.6:
- JT Middleware Login

## 2.1.5:
- Add support for Trigana Air V2

## 2.1.4:
- Add JT Middleware Login

## 2.1.3:
- Add support for TSY Aggregator

## 2.1.2:
- Split logout tools and airline provider
- Implement order cache storage
- Support Login OTP

## 2.1.1:
- Revert default authentication method from Cognito w/ Multi-factor authentication (MFA) to Cognito
- Fix unififi support bug
- Add support for PKFare

## 2.1.0:
- Replace default authentication method from Cognito to Cognito w/ Multi-factor authentication (MFA)
- Remove Auth0 authentication method
- Add support for Vietnam Airlines, Onefly, Unififi

## 2.0.0:
- Update manifest version to v3
- Support for multiple chunk responses
- Fix some bugs

## 1.1.11:
- New Login Mechanism

## 1.1.2:
- Latest Working Version + Cognito Auth

## 1.0.8:
- Latest Working Version